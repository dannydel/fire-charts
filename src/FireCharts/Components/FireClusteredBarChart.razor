@namespace FireCharts.Components
@typeparam TItem
@typeparam TSegment
@using FireCharts.Models

<CategoricalBarChartCore TItem="TItem"
                         TSegment="TSegment"
                         Title="@Title"
                         Description="@Description"
                         Items="@Items"
                         SegmentsSelector="@SegmentsSelector"
                         LabelSelector="@LabelSelector"
                         SegmentValueSelector="@SegmentValueSelector"
                         SegmentLabelSelector="@SegmentLabelSelector"
                         SegmentColorSelector="@SegmentColorSelector"
                         SegmentHoverColorSelector="@SegmentHoverColorSelector"
                         Width="@Width"
                         Height="@Height"
                         Responsive="@Responsive"
                         Horizontal="@Horizontal"
                         ShowGridLines="@ShowGridLines"
                         ShowAxisLabels="@ShowAxisLabels"
                         ShowTooltip="@ShowTooltip"
                         ConstrainTooltipToChartBounds="@ConstrainTooltipToChartBounds"
                         ShowLegend="@ShowLegend"
                         GridLineCount="@GridLineCount"
                         BarWidthRatio="@BarWidthRatio"
                         GroupSpacing="@GroupSpacing"
                         SeriesSpacing="@SeriesSpacing"
                         ValueFormat="@ValueFormat"
                         MaxValue="@MaxValue"
                         CornerRadius="@CornerRadius"
                         LegendPlacement="@LegendPlacement"
                         TooltipInteractionMode="@TooltipInteractionMode"
                         Layout="LayoutStrategy"
                         CssPrefix="clustered"
                         IsSegmentSelected="IsSegmentSelectedCore"
                         OnSegmentActivated="HandleSegmentActivatedAsync"
                         OnSegmentHovered="HandleSegmentHoveredAsync"
                         TooltipTemplate="CoreTooltipTemplate"
                         SharedTooltipTemplate="CoreSharedTooltipTemplate"
                         ChartContentTemplate="CoreChartContentTemplate"
                         EmptyStateTemplate="@EmptyStateTemplate" />
